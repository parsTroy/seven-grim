import React from 'react'
import styles from '../../style'

const NewsLetter = () => (
    <div className={`bg-primary ${styles.flexStart}`}>'
        <div className={`${styles.boxWidth}`}>
            <p className='text-white text-4xl'>NEWSLETTER</p>
        </div>
    </div>
  )

export default NewsLetter