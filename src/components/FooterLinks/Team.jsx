import React from 'react'
import styles from '../../style'

const Team = () => (
    <div className={`bg-primary ${styles.flexStart}`}>'
        <div className={`${styles.boxWidth}`}>
            <p className='text-white text-4xl'>OUR TEAM</p>
        </div>
    </div>
  )

export default Team