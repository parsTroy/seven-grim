import React from 'react'
import styles from '../../style'

const Explore = () => (
    <div className={`bg-primary ${styles.flexStart}`}>'
        <div className={`${styles.boxWidth}`}>
            <p className='text-white text-4xl'>EXPLORE</p>
        </div>
    </div>
  )

export default Explore